@main(cmd_args) {
	declare $count 0;
	declare $count (add $count 2);

	print $count;

	declare $count 0;

	print (cmp_gr 1000 $count);

	while(cmp_gr 1000 $count) {
		declare $count (add 1 $count);
		print $count;
	}
}

@second(arg1 arg2) {
	print $arg1 $arg2;
	add 3 3
}

@arguments(arg1 arg2) {
	print $arg1 $arg2;
}